# 재개 안내 (2026-09-12 일시정지 시점 기준)

정전으로 서버가 꺼지기 전 상태를 기록해 둔다. 대화 기록 없이도 이 문서만으로 이어갈 수 있다.

## 지금 어디까지 왔나

1.2B 한국어 모델 사전학습이 **step 19,200 / 24,000 (80%)** 에서 멈춰 있다.
step 19,200은 annealing(학습률 decay 구간) 시작 지점이라, 저장된 체크포인트는
**고품질 데이터를 보기 직전** 상태다.

```
모델      1.2B (dim 2048 / 24층 / GQA 16:4 / ctx 2048 / vocab 65,536)
학습량    20.1B 토큰
남은 양   4,800 step ≈ 5.03B 토큰 ≈ 5.5일 (11K tok/s 기준)
```

### 성적 (step 19,200)

<!-- logs/milestones_base_1b.csv 전체 이력 -->

| 토큰 | perplexity | COPA | BoolQ | HellaSwag |
|---|---|---|---|---|
| 4.2B | 24.18 | 58.0% | 46.7%* | 36.0% |
| 8.4B | 19.33 | 60.7% | 46.7%* | 40.7% |
| 12.6B | 17.77 | 60.7% | 46.7%* | 40.7% |
| 16.8B | 16.62 | 64.0% | 46.7%* | 43.3% |
| **20.1B** | 17.02 | 62.7% | **56.7%** | **46.7%** |
| 무작위 | — | 50% | 50% | 25% |

`*` 표시는 모델이 150문항 전부 한쪽으로 답해 점수가 라벨 분포일 뿐이었던 구간.
20.1B에서 처음으로 예측이 갈려 유효한 값이 나왔다.

perplexity가 16.62 → 17.02로 오른 것은 성능 저하가 아니다. val 셋이 웹 중심인데
모델이 위키·교과서 위주로 학습을 시작했기 때문이다. 완주 후에는 웹/고품질 두
val 셋 모두로 재야 한다.

## 서버에 있는 것 (git으로 백업 불가, 용량 때문)

```
~/llm_from_scratch/
├── checkpoints/base_1b/
│   ├── latest.pt          14G  step 19,200  ← 재개는 이 파일에서
│   ├── step_019200.pt     14G  annealing 직전 상태 (비교 기준용 보존)
│   ├── step_016000.pt     14G
│   ├── step_012000.pt     14G
│   ├── step_008000.pt     14G
│   └── step_004000.pt     14G
├── data/shards_26b/       45G  본 학습 데이터 23.5B 토큰
└── data/shards_anneal/    9.3G annealing 데이터 4.94B 토큰
```

2026-09-12에 6개 체크포인트 전부 무결성 확인했다 — 모델 267 텐서, 옵티마이저 266개
상태, 가중치에 NaN/Inf 없음.

## 재개 방법

```bash
ssh sangmin@192.168.0.11
cd llm_from_scratch
./resume.sh
```

`resume.sh`가 `latest.pt`에서 이어받고, 학습 감시(`run_training.sh`)와 마일스톤
자동 평가(`watch_milestones.sh`)를 함께 켠다. step 19,201부터 시작하며 **재개 즉시
annealing 데이터로 전환**된다. 로그에 이렇게 찍히면 정상이다.

```
재개: step 19201부터
=== step 19201: annealing 데이터로 전환 (data/shards_anneal) ===
```

첫 진행 로그는 `torch.compile` 워밍업 때문에 20분쯤 뒤에 나온다.

### 재개 전 확인할 것

**GPU가 비어 있어야 한다.** 이 GPU는 다른 연구(vLLM 추론 서버, RAG 파이프라인)와
공유되며 그쪽이 14~16GB를 쓴다. 우리 학습은 26GB를 쓰므로 겹치면 OOM 크래시
루프에 빠진다 (2026-08-06에 실제로 발생).

```bash
nvidia-smi --query-compute-apps=pid,used_memory --format=csv
```

## 완주 후 할 일

1. **전체 평가** — 문항 수 제한 없이 KOBEST 3종 + perplexity(웹/고품질 두 val 셋)
2. **SFT** — `python sft.py --base checkpoints/base_1b/latest.pt --config sft_400m`
   (config의 model을 base_1b로 바꿔야 함)
3. **대화 데모** — `python sample.py --ckpt checkpoints/sft_*/latest.pt --chat`

## 알아둘 함정들

- **코퍼스 크기를 추정하지 말 것.** parquet 압축 크기와 스트리밍 앞부분 표본이
  각각 3배, 5배 빗나갔다. 실제 준비 실행의 산출량만 믿을 것.
- **중복 제거는 소스별로 켠다.** 위키·교과서처럼 의도적으로 반복 투입하는 코퍼스에
  걸면 2회차부터 전부 버려진다.
- **벤치마크는 점수와 예측 분포를 함께 본다.** 한쪽으로 쏠리면 점수는 라벨 분포일 뿐이다.
- **체크포인트 하나로 판단하지 말 것.** val loss가 100 step 주기로 ±0.1 진동해서,
  하필 고점 체크포인트를 뽑으면 모델이 퇴보한 것처럼 보인다.
